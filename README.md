# Python BlueCoat Log Parser

Python script to parse bluecoat proxy logs using multiprocessing for performance.

Run as follows, but make sure to adjust the field names and order in the script to match your BlueCoat log:

```sh
$ python3 <path>/<logfile>
```
